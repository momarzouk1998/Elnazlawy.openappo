## خطة: تحسين قائمة الأوردرات في فورم اليومية

### المشكلة:
قائمة الأوردرات في فورم اليومية بتعرض آخر 100 بس (`limit=100`) بدون بحث — الأوردرات مش ظاهرة كلها

### التعديل في `_new-journal-form.tsx`:
1. تغيير `/api/orders?limit=100` → `/api/orders?limit=500`
2. تحويل الـ `<select>` لـ `<input>` مع `<datalist>` + بحث بالاسم — بدل لستة طويلة بدون بحث